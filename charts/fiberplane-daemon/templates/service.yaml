apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiberplane-daemon.fullname" . }}
  labels:
    {{- include "fiberplane-daemon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiberplane-daemon.selectorLabels" . | nindent 4 }}
