apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiberplane-daemon.fullname" . }}-test-connection"
  labels:
    {{- include "fiberplane-daemon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fiberplane-daemon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
